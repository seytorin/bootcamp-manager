const React = require("react-native");

const { StyleSheet } = React;

export default {
  container: {
    backgroundColor: "#FFF"
  },
  mb: {
    marginBottom: 10
  },
  flatListItem: {
    color: 'white',
    padding: 10,
    fontSize: 16,  
}
};
